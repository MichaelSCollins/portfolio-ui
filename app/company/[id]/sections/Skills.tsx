
export const Skills = ({ }: { skills: number[] }) => {
    return (<section className={'flex flex-col'}>
        {/* <ul>
            {skills.map(
                (skill, i) => (<li key={i}>
                    {skill}
                </li>))}
        </ul> */}
    </section>)
}
